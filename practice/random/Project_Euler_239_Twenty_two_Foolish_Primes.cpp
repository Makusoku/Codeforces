#include <bits/stdc++.h>
using namespace std;
#define ll long long

ll S (ll i) {
    return i;
}

void solve () {
    ll n, k; cin >> n >> k;

    
}

int main () {
    ios::sync_with_stdio(0);
    cin.tie(0);
    ll _ = 1;
    cin >> _;
    while (_--)
        solve ();
    return 0;
}